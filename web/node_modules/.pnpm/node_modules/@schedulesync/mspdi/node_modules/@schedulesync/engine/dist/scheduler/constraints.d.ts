/**
 * Constraint handling for the CPM scheduler.
 * Mirrors MS Project's PjConstraint behaviour.
 */
import { type Dayjs } from 'dayjs';
import { ConstraintType, type ScheduleWarning } from '../types.js';
export interface ConstraintResult {
    /** Adjusted early start after applying the constraint. */
    earlyStart: Dayjs;
    /** Adjusted early finish after applying the constraint. */
    earlyFinish: Dayjs;
    /** Warnings generated by constraint application. */
    warnings: ScheduleWarning[];
}
/**
 * Apply constraint to the computed early start/finish during forward pass.
 * Some constraints (ALAP) are only meaningful after the backward pass and
 * are handled separately by applyALAPConstraint.
 */
export declare function applyConstraintForwardPass(taskId: string, constraintType: ConstraintType, constraintDate: string | null, earlyStart: Dayjs, earlyFinish: Dayjs, durationMinutes: number, addWorkingMinutesFn: (start: Dayjs, minutes: number) => Dayjs, subtractWorkingMinutesFn: (end: Dayjs, minutes: number) => Dayjs): ConstraintResult;
/**
 * Apply constraint during backward pass for late dates.
 */
export declare function applyConstraintBackwardPass(constraintType: ConstraintType, constraintDate: string | null, lateStart: Dayjs, lateFinish: Dayjs, durationMinutes: number, addWorkingMinutesFn: (start: Dayjs, minutes: number) => Dayjs, subtractWorkingMinutesFn: (end: Dayjs, minutes: number) => Dayjs): {
    lateStart: Dayjs;
    lateFinish: Dayjs;
};
//# sourceMappingURL=constraints.d.ts.map