/**
 * AI Service — integrates with a local Ollama instance for LLM-powered
 * scheduling advice, task suggestions, and pattern-based learning.
 */
/** Check if Ollama is reachable. */
export declare function checkOllamaHealth(): Promise<boolean>;
/** List available models on the local Ollama instance. */
export declare function listModels(): Promise<string[]>;
/**
 * Chat with the AI about a project. Maintains conversation history in the database.
 */
export declare function chat(projectId: string, userMessage: string, conversationId?: string): Promise<{
    conversationId: string;
    response: string;
}>;
/**
 * Get a quick AI suggestion for a specific action (non-conversational).
 */
export declare function suggest(projectId: string, suggestionType: 'duration' | 'dependency' | 'name' | 'resource' | 'general', context: Record<string, unknown>): Promise<string>;
/**
 * Record user feedback on an AI suggestion (accepted/rejected/corrected).
 */
export declare function recordFeedback(projectId: string, suggestionType: string, suggestion: Record<string, unknown>, accepted: boolean, correctedValue?: string): Promise<void>;
//# sourceMappingURL=aiService.d.ts.map