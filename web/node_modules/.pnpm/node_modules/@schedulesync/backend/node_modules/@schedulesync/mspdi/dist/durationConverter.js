/**
 * ISO 8601 duration ↔ minutes conversion for MSPDI.
 *
 * MSPDI uses the format: PT{hours}H{minutes}M{seconds}S
 * Example: PT8H0M0S = 480 minutes = 1 work day
 */
/**
 * Parse an ISO 8601 duration string to total minutes.
 * Handles: PT8H0M0S, PT480H0M0S, P1D (1 day = 1440 minutes), etc.
 */
export function durationToMinutes(isoDuration) {
    if (!isoDuration)
        return 0;
    let totalMinutes = 0;
    const str = isoDuration.trim();
    // Match P[n]DT[n]H[n]M[n]S pattern
    const dayMatch = str.match(/(\d+)D/);
    const hourMatch = str.match(/(\d+)H/);
    const minuteMatch = str.match(/T.*?(\d+)M/);
    const secondMatch = str.match(/(\d+)S/);
    if (dayMatch)
        totalMinutes += parseInt(dayMatch[1], 10) * 24 * 60;
    if (hourMatch)
        totalMinutes += parseInt(hourMatch[1], 10) * 60;
    if (minuteMatch)
        totalMinutes += parseInt(minuteMatch[1], 10);
    if (secondMatch)
        totalMinutes += parseInt(secondMatch[1], 10) / 60;
    return totalMinutes;
}
/**
 * Convert minutes to ISO 8601 duration string.
 * Uses the PT{H}H{M}M{S}S format that MSPDI expects.
 */
export function minutesToDuration(minutes) {
    const hours = Math.floor(minutes / 60);
    const mins = Math.floor(minutes % 60);
    const secs = Math.round((minutes - Math.floor(minutes)) * 60);
    return `PT${hours}H${mins}M${secs}S`;
}
/**
 * Convert MSPDI link lag (tenths of minutes) to minutes.
 * MSPDI stores lag as integer tenths-of-minutes.
 */
export function lagFromMspdi(lagTenths) {
    return lagTenths / 10;
}
/**
 * Convert minutes to MSPDI link lag (tenths of minutes).
 */
export function lagToMspdi(lagMinutes) {
    return Math.round(lagMinutes * 10);
}
//# sourceMappingURL=durationConverter.js.map