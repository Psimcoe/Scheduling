/**
 * Earned Value Engine — computes EV metrics for tasks and the project.
 *
 * Implements standard Earned Value Management (EVM):
 * - BCWS (Budgeted Cost of Work Scheduled) = BAC × (planned % complete at status date)
 * - BCWP (Budgeted Cost of Work Performed) = BAC × (actual % complete)
 * - ACWP (Actual Cost of Work Performed) = actual cost incurred
 * - SV = BCWP - BCWS  (Schedule Variance)
 * - CV = BCWP - ACWP  (Cost Variance)
 * - SPI = BCWP / BCWS (Schedule Performance Index)
 * - CPI = BCWP / ACWP (Cost Performance Index)
 * - EAC = BAC / CPI   (Estimate At Completion)
 * - VAC = BAC - EAC   (Variance At Completion)
 */
import type { Task, Baseline, ProjectData } from '../types.js';
export interface EarnedValueResult {
    taskId: string;
    bac: number;
    bcws: number;
    bcwp: number;
    acwp: number;
    sv: number;
    cv: number;
    spi: number;
    cpi: number;
    eac: number;
    vac: number;
}
export interface ProjectEVSummary {
    totalBAC: number;
    totalBCWS: number;
    totalBCWP: number;
    totalACWP: number;
    sv: number;
    cv: number;
    spi: number;
    cpi: number;
    eac: number;
    vac: number;
    taskResults: EarnedValueResult[];
}
/**
 * Compute earned value for a single task.
 */
export declare function computeTaskEV(task: Task, baseline: Baseline | undefined, statusDate: string): EarnedValueResult;
/**
 * Compute earned value for all tasks and produce project summary.
 */
export declare function computeProjectEV(project: ProjectData, statusDate?: string): ProjectEVSummary;
//# sourceMappingURL=earnedValue.d.ts.map