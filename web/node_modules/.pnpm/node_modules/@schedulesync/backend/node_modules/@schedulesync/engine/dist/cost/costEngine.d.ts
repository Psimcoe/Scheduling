/**
 * Cost Engine — calculates task/assignment costs based on resource rates,
 * accrual methods, and rate tables.
 *
 * Implements MS Project's cost calculation model:
 * - Work resources: cost = (work hours) × (rate) + costPerUse
 * - Material resources: cost = units × rate
 * - Cost resources: cost = assignment cost (no rate)
 * - Fixed costs accrue based on accrual method (start/end/prorated)
 */
import type { Task, Assignment, Resource, CostRateTable, CostRateEntry, ProjectData } from '../types.js';
/**
 * Get the applicable rate entry from a cost rate table at a given date.
 * Returns the entry whose effectiveDate is on or before the given date.
 */
export declare function getApplicableRate(rateTables: CostRateTable[] | undefined, tableIndex: number, date: string): CostRateEntry;
export interface AssignmentCostResult {
    cost: number;
    actualCost: number;
    remainingCost: number;
}
export declare function calculateAssignmentCost(assignment: Assignment, resource: Resource): AssignmentCostResult;
export interface TaskCostResult {
    cost: number;
    actualCost: number;
    remainingCost: number;
    work: number;
    actualWork: number;
    remainingWork: number;
}
/**
 * Calculate total cost for a task based on its assignments and fixed cost.
 */
export declare function calculateTaskCost(task: Task, assignments: Assignment[], resources: Resource[]): TaskCostResult;
/**
 * Calculate costs for all tasks in a project.
 * Returns updated tasks with cost fields populated.
 */
export declare function rollupProjectCosts(project: ProjectData): Task[];
//# sourceMappingURL=costEngine.d.ts.map