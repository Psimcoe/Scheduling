/**
 * Critical path computation.
 *
 * A task is critical when its total slack ≤ 0.
 * Total slack = Late Finish − Early Finish (in working minutes).
 * Free slack = min successor Early Start − this task Early Finish − lag (for FS).
 */
import type { Dayjs } from 'dayjs';
import { type Calendar, type Dependency, type Task } from '../types.js';
export interface SlackInfo {
    totalSlackMinutes: number;
    freeSlackMinutes: number;
    isCritical: boolean;
}
/**
 * Compute slack values and critical flag for all tasks.
 */
export declare function computeSlack(taskMap: Map<string, Task>, earlyDates: Map<string, {
    earlyStart: Dayjs;
    earlyFinish: Dayjs;
}>, lateDates: Map<string, {
    lateStart: Dayjs;
    lateFinish: Dayjs;
}>, depsByPredecessor: Map<string, Dependency[]>, calendarMap: Map<string, Calendar>, defaultCalendar: Calendar): Map<string, SlackInfo>;
//# sourceMappingURL=criticalPath.d.ts.map